spring:
  datasource:
    url: jdbc:postgresql://db.suoiyaaswcibsbrvpjxa.supabase.co:5432/postgres
    username: postgres
    password: 1Mj4WSc8KcsByjqL
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: schoolmate-production-jwt-secret-key-2026-secure-minimum-256-bits-for-hs256
  expiration: 86400000

logging:
  level:
    com.schoolmate: INFO
